import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  symbolsContainer: {
    flex: 1,
    flexDirection: 'row',
  },
  image: {
    flex: 1,
    aspectRatio: 1.5,
    resizeMode: 'contain',
  },
});
